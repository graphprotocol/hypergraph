{
  "name": "graph-framework-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "ts:check": "pnpm -r ts:check",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
