{
  "name": "graph-framework-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "escape-string-regexp": "^5.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4",
    "vite-plugin-dts": "^4.3.0"
  },
  "scripts": {
    "build": "pnpm --filter graph-framework-utils --filter graph-framework-identity --filter graph-framework-schema --filter graph-framework-space-events --filter graph-framework-identity --filter graph-framework build",
    "ts:check": "pnpm -r ts:check",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
